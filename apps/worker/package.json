{
  "name": "@kentos/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/main.ts",
    "start": "node dist/main.js",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm typecheck && tsx --test src/processors/media.processor.test.ts src/processors/retention.processor.test.ts src/scan/clamav-client.test.ts"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.799.0",
    "@kentos/shared": "workspace:*",
    "@kentos/database": "workspace:*",
    "bullmq": "^5.34.2",
    "ioredis": "^5.4.2"
  }
}
